import { TitlePage } from "@/utils/TitlePage";
import React from "react";

const Seminar = () => {
  TitlePage("Seminar");

  return (
    <React.Fragment>
      <div>Seminar</div>
    </React.Fragment>
  );
};

export default Seminar;
