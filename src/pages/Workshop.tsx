import { TitlePage } from "@/utils/TitlePage";
import React from "react";

const Workshop = () => {
  TitlePage("Workshop");

  return (
    <React.Fragment>
      <div>Workshop</div>
    </React.Fragment>
  );
};

export default Workshop;
